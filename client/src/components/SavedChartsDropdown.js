import React from 'react';

const SavedChartsDropdown = () => {
    return (
        <select defaultValue="Saved Charts">
            <option disabled>Saved Charts</option>
        </select>
    );
};

export default SavedChartsDropdown;